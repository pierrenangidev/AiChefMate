@import "variables.styl"
@import "mixins.styl"
@import "reset.styl"

html, 
body,
#app
  overflow-x hidden
  
body
  font fontSize workSans 
  width 100vw
  display flex
  flex-direction column
  align-items center
  color primaryGreen
  background backgroundLight

#app
  padding 70px 0 0 0
  background backgroundLight
  width 100%
  height 100%
  display flex
  flex-direction column
  align-items center
  justify-content space-between
  color primaryGreen

footer
  width 100%